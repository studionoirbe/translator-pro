{% extends "_layouts/cp" %}
{% import "_includes/forms.twig" as forms %}

{% set selectedSubnavItem = 'static' %}
{% set title = "Static translations"|t('translingua') %}

{% block actionButton %}
    <form method="post" accept-charset="UTF-8">
        {{ csrfInput() }}
        {{ actionInput('translingua/static/rescan') }}
        {{ redirectInput('translingua/static') }}
        <button type="submit" class="btn">{{ "Rescan templates"|t('translingua') }}</button>
    </form>
{% endblock %}

{% block content %}
    <p class="light">
        {{ "Translations are written to {path}. Craft loads that folder on top of each plugin’s own language files, so nothing inside vendor/ is ever touched."|t('translingua', {
            path: '<code>translations/{locale}/{category}.php</code>'
        })|raw }}
    </p>

    {% if sources is empty %}
        <p>{{ "No translation sources found."|t('translingua') }}</p>
    {% else %}
        <table class="data fullwidth">
            <thead>
                <tr>
                    <th scope="col">{{ "Source"|t('translingua') }}</th>
                    <th scope="col">{{ "Category"|t('translingua') }}</th>
                    <th scope="col">{{ "Strings"|t('translingua') }}</th>
                    {% for locale, label in locales %}
                        <th scope="col">{{ locale }}</th>
                    {% endfor %}
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                {% for source in sources %}
                    {% set typeLabel = {
                        site: "Templates"|t('translingua'),
                        app: "Craft"|t('translingua'),
                        plugin: "Plugin"|t('translingua'),
                    }[source.type] ?? source.type %}
                    <tr>
                        <th scope="row">
                            <a href="{{ source.cpEditUrl }}">{{ source.name }}</a>
                            <span class="light">— {{ typeLabel }}</span>
                        </th>
                        <td><code>{{ source.category }}</code></td>
                        <td>{{ source.stringCount }}</td>
                        {% for locale, label in locales %}
                            {% set missing = translations.getMissingCount(source, locale) %}
                            <td>
                                {% if missing == 0 %}
                                    <span class="success">{{ "Complete"|t('translingua') }}</span>
                                {% else %}
                                    <span class="light">{{ "{n} missing"|t('translingua', { n: missing }) }}</span>
                                {% endif %}
                            </td>
                        {% endfor %}
                        <td class="thin">
                            <a class="btn small" href="{{ source.cpEditUrl }}">{{ "Edit"|t('translingua') }}</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endif %}

    <h2 style="margin-top: 32px;">{{ "Scanned for template strings"|t('translingua') }}</h2>
    <ul class="light">
        {% for path in scanPaths %}
            <li><code>{{ path }}</code></li>
        {% else %}
            <li>{{ "No scannable directories were found."|t('translingua') }}</li>
        {% endfor %}
    </ul>
{% endblock %}
